{
  "name": "content-ops",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.7.0",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "analyze": "pnpm --filter @content-ops/cli run analyze",
    "web": "pnpm --filter @content-ops/web run dev",
    "report": "pnpm --filter @content-ops/cli run report",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
